# frozen_string_literal: true

source "https://rubygems.org"

# Pinned to lich-5's Gemfile.lock so specs exercise the same gems the daemon runs.
gem "redis", "5.4.1"
gem "redis-client", "0.28.0"

group :development do
  gem "rspec", "~> 3.13"
end
